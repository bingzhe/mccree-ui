import styled from "styled-components";
import { th, variant, system, breakpoints, color, backgroundColor } from "@xstyled/system";
import { getSystemPropTypes } from "@xstyled/prop-types";

export { styled, system, th, variant, breakpoints, color, backgroundColor, getSystemPropTypes };

export * from "styled-components";
