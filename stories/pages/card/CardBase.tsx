import * as React from "react";
import { Card } from "../../../components/index";

const CardBase = () => {
    return (
        <Card width="400px">
            <p>一个基础的卡片。</p>
        </Card>
    );
};

export default CardBase;
